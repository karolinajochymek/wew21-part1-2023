import { SimpleCheckPermissionDirective } from './simple-check-permission.directive';

describe('SimpleCheckPermissionDirective', () => {
  it('should create an instance', () => {
    const directive = new SimpleCheckPermissionDirective();
    expect(directive).toBeTruthy();
  });
});
