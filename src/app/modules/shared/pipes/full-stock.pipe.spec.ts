import { FullStockPipe } from './full-stock.pipe';

describe('FullStockPipe', () => {
  it('create an instance', () => {
    const pipe = new FullStockPipe();
    expect(pipe).toBeTruthy();
  });
});
