import { UpperCaseByLastIndxPipe } from './upper-case-by-last-indx.pipe';

describe('UpperCaseByLastIndxPipe', () => {
  it('create an instance', () => {
    const pipe = new UpperCaseByLastIndxPipe();
    expect(pipe).toBeTruthy();
  });
});
